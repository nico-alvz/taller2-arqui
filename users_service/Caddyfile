:10000 {
    # Health checks
    respond / "OK - Render health check"
    respond /healthz "OK - Render health check"

    # Reverse proxy gRPC (h2c)
    reverse_proxy 127.0.0.1:50052 {
        transport http {
            versions h2c
        }
    }
}
